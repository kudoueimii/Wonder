 <%= form_with model: @user_choice, url: fourth_user_steps_path, method: :get do |form| %>
  <h6 class="which_one"><%= t('.Notice')%></h6>
  <p class="enum-choice"><%= t('.Active day off')%></p>
  
  <div class="enum-choice">
    <%= form.radio_button :active, :like, class: "radio-inline__input" %>
    <%= form.label(:active, (t'.Like'), value: "like", class: "radio-inline__label") %>
    <%= form.radio_button :active, :dislike, class: "radio-inline__input" %>
    <%= form.label(:active, (t'.Dislike'), value: "dislike", class: "radio-inline__label") %>
  </div>
  
  <div class="actions">
    <%= form.submit (t '.Next'), class: "button004" %>
  </div>


  <div class="step_back">
    <%= link_to t('.Back'), second_user_steps_path, class: "text-info" %>
  </div>

  <div class="tip">
    <p><%= t('.Have you ever heard of sheepdogs?') %></p>
    <p><%= t('.They are responsible for bringing grazing sheep back together to pasture,bringing lost sheep back to the flock,') %></p>
    <p><%= t('.and, They are physically strong and good at using their brains') %></p>
    <p><%= t('.There are other herding dogs, hounds, etc') %></p>
    <p><%= t('.There are so many of them that are active in dog sports,') %></p>
    <p><%= t('.You may want to look into dog sports') %></p>
  </div>

<% end %>