<div class="white-frame">
  <div class="show_title">
    <div><h2><%= t('.dog show')%><h2></div>
    <div class="container">
      <div class="row">
        <div class="col-6">
          <%= image_tag @dog.image_url, size: '500x400'%>
        </div>
        <div class="col-6">
          <div class="row">
            <div class="col-8 col-sm-6">
              <strong><%= t('.Name')%></strong><br>
              <%= t('.' + @dog.name) %>
            </div>
            <div class="col-4 col-sm-6">
              <% if @user_favorite.present? %>
                <br>
                <%= link_to '💜', favorite_path(id: @user_favorite.id), method: :delete %>
              <% else %>
                <br>
                <%= link_to '💟', favorites_path(dog_id: @dog.id), method: :post %>
              <% end %>
            </div>
          </div>
          <br><br>
          <div>
            <strong><%= t('.Size')%></strong><br>
            <%= @dog.size_i18n %>
          </div>
          <br><br>
          <div><strong><%= t('.Feature')%></strong><br>
            <%= @dog.home_i18n %>
          </div>
          <br><br>
          <div><strong><%= t('.Cleaning') %></strong>
            <%= @dog.cleaning_i18n %>
          <div>
          <br><br>
          <div><strong><%= t('.Active')%></strong><br>
            <%= @dog.active_i18n %></div>
        </div>

      </div>
    </div>
  </div>
  <div class="show-back">
    <%= link_to t('.Back'), dogs_path %>
  </div>
</div>